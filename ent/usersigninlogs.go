// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/PokeForum/PokeForum/ent/usersigninlogs"
)

// UserSigninLogs is the model entity for the UserSigninLogs schema.
type UserSigninLogs struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// User ID | 用户ID
	UserID int64 `json:"user_id,omitempty"`
	// Sign-in date | 签到日期
	SignDate     time.Time `json:"sign_date,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserSigninLogs) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case usersigninlogs.FieldID, usersigninlogs.FieldUserID:
			values[i] = new(sql.NullInt64)
		case usersigninlogs.FieldCreatedAt, usersigninlogs.FieldUpdatedAt, usersigninlogs.FieldSignDate:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserSigninLogs fields.
func (_m *UserSigninLogs) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case usersigninlogs.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case usersigninlogs.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case usersigninlogs.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case usersigninlogs.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.Int64
			}
		case usersigninlogs.FieldSignDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field sign_date", values[i])
			} else if value.Valid {
				_m.SignDate = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserSigninLogs.
// This includes values selected through modifiers, order, etc.
func (_m *UserSigninLogs) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this UserSigninLogs.
// Note that you need to call UserSigninLogs.Unwrap() before calling this method if this UserSigninLogs
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserSigninLogs) Update() *UserSigninLogsUpdateOne {
	return NewUserSigninLogsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserSigninLogs entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserSigninLogs) Unwrap() *UserSigninLogs {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserSigninLogs is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserSigninLogs) String() string {
	var builder strings.Builder
	builder.WriteString("UserSigninLogs(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("sign_date=")
	builder.WriteString(_m.SignDate.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserSigninLogsSlice is a parsable slice of UserSigninLogs.
type UserSigninLogsSlice []*UserSigninLogs
