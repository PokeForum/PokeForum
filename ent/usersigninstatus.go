// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/PokeForum/PokeForum/ent/usersigninstatus"
)

// UserSigninStatus is the model entity for the UserSigninStatus schema.
type UserSigninStatus struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 用户ID
	UserID int64 `json:"user_id,omitempty"`
	// 最近签到日期
	LastSigninDate time.Time `json:"last_signin_date,omitempty"`
	// 连续签到天数
	ContinuousDays int `json:"continuous_days,omitempty"`
	// 累计签到天数
	TotalDays    int `json:"total_days,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserSigninStatus) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case usersigninstatus.FieldID, usersigninstatus.FieldUserID, usersigninstatus.FieldContinuousDays, usersigninstatus.FieldTotalDays:
			values[i] = new(sql.NullInt64)
		case usersigninstatus.FieldCreatedAt, usersigninstatus.FieldUpdatedAt, usersigninstatus.FieldLastSigninDate:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserSigninStatus fields.
func (_m *UserSigninStatus) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case usersigninstatus.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case usersigninstatus.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case usersigninstatus.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case usersigninstatus.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.Int64
			}
		case usersigninstatus.FieldLastSigninDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_signin_date", values[i])
			} else if value.Valid {
				_m.LastSigninDate = value.Time
			}
		case usersigninstatus.FieldContinuousDays:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field continuous_days", values[i])
			} else if value.Valid {
				_m.ContinuousDays = int(value.Int64)
			}
		case usersigninstatus.FieldTotalDays:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field total_days", values[i])
			} else if value.Valid {
				_m.TotalDays = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserSigninStatus.
// This includes values selected through modifiers, order, etc.
func (_m *UserSigninStatus) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this UserSigninStatus.
// Note that you need to call UserSigninStatus.Unwrap() before calling this method if this UserSigninStatus
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserSigninStatus) Update() *UserSigninStatusUpdateOne {
	return NewUserSigninStatusClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserSigninStatus entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserSigninStatus) Unwrap() *UserSigninStatus {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserSigninStatus is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserSigninStatus) String() string {
	var builder strings.Builder
	builder.WriteString("UserSigninStatus(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("last_signin_date=")
	builder.WriteString(_m.LastSigninDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("continuous_days=")
	builder.WriteString(fmt.Sprintf("%v", _m.ContinuousDays))
	builder.WriteString(", ")
	builder.WriteString("total_days=")
	builder.WriteString(fmt.Sprintf("%v", _m.TotalDays))
	builder.WriteByte(')')
	return builder.String()
}

// UserSigninStatusSlice is a parsable slice of UserSigninStatus.
type UserSigninStatusSlice []*UserSigninStatus
